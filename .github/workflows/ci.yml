# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActionsSteps (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_ci --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: ci

on:
  push:
    branches:
      - master
      - du-fuk
    tags:
      - v*
  pull_request:
    branches:
      - master
      - du-fuk

jobs:
  Build:
    strategy:
      fail-fast: false
      matrix:
        os: [macOS-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Install GitVersion
        uses: david-driscoll/gittools-actions/gitversion/setup@feature/export-environment-github
        with:
          versionSpec: '5.1.x'
      - name: Use GitVersion
        id: gitversion
        uses: david-driscoll/gittools-actions/gitversion/execute@feature/export-environment-github
      - name: '🛠 DotnetToolRestore'
        shell: pwsh
        run: ./build.ps1 DotnetToolRestore --skip
      - name: '📪 Restore'
        shell: pwsh
        run: ./build.ps1 BuildVersion Clean Restore --skip
      - name: '⚙ Build'
        shell: pwsh
        run: ./build.ps1 Build --skip
      - name: '🚦 Test'
        shell: pwsh
        run: ./build.ps1 Generate_Code_Coverage_Reports Test --skip
      - name: '📦 Pack'
        shell: pwsh
        run: ./build.ps1 Pack --skip
      - name: Publish Coverage
        uses: codecov/codecov-action@v1
        with:
          file: 'coverage/solution.xml'
          name: 'actions-${{ matrix.os }}'
          fail_ci_if_error: 'true'
      - uses: actions/upload-artifact@v1
        name: Publish logs
        with:
          name: logs
          path: ${{ env.GITHUB_WORKSPACE }}/artifacts/logs/
      - uses: actions/upload-artifact@v1
        name: Publish coverage data
        with:
          name: coverage
          path: ${{ env.GITHUB_WORKSPACE }}/coverage/
      - uses: actions/upload-artifact@v1
        name: Publish NuGet Packages
        with:
          name: nuget
          path: ${{ env.GITHUB_WORKSPACE }}/artifacts/nuget/
