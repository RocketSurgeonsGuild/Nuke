codecov:
    require_ci_to_pass: no
    bot: rsg-bot
    strict_yaml_branch: master

coverage:
    round: up
    precision: 1

    status:
        project: yes
        patch: yes
        changes: no

comment:
    branch: master
